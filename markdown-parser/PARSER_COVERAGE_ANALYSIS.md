# Markdown 解析器功能覆盖分析

## 1. 块级结构 — 标题

### ❌ 待实现

#### ATX 标题
- ❌ `# heading` 1 级标题
- ❌ `## heading` 2 级标题
- ❌ `### heading` 3 级标题
- ❌ `#### heading` 4 级标题
- ❌ `##### heading` 5 级标题
- ❌ `###### heading` 6 级标题
- ❌ 标题尾部可选 `#` 闭合（`# heading #`）
- ❌ 标题前最多 3 个空格缩进
- ❌ `#` 后必须有空格或行尾（`#heading` 不是标题）

#### Setext 标题
- ❌ `heading\n===` Setext 标题（`=` 下划线，1 级）
- ❌ `heading\n---` Setext 标题（`-` 下划线，2 级）
- ❌ 下划线长度 ≥ 1
- ❌ 下划线前最多 3 个空格缩进
- ❌ Setext 标题可以跨多行内容

#### 标题 ID（扩展）
- ❌ `### heading {#custom-id}` 自定义标题 ID
- ❌ 自动生成标题 ID（基于标题文本）
- ❌ 标题锚点链接 `[link](#custom-id)`

### ✅ 已支持
- 无

**覆盖率**: 0/17 (0%)

---

## 2. 块级结构 — 段落与空行

### ❌ 待实现
- ❌ 连续非空行合并为段落
- ❌ 空行分隔段落
- ❌ 段落的 lazy continuation（块引用/列表中的延续行）
- ❌ 段落首行前最多 3 个空格不算缩进代码块
- ❌ 空行（blank line）：只包含空格/Tab 的行视为空行

### ✅ 已支持
- 无

**覆盖率**: 0/5 (0%)

---

## 3. 块级结构 — 代码块

### ❌ 待实现

#### 围栏代码块
- ❌ `` ``` `` 反引号围栏（≥ 3 个）
- ❌ `~~~` 波浪线围栏（≥ 3 个）
- ❌ 围栏后 info string（语言标识，如 `` ```kotlin ``）
- ❌ info string 仅取第一个词作为语言
- ❌ 闭合围栏长度 ≥ 开启围栏长度
- ❌ 闭合围栏字符须与开启围栏相同（不能混用 `` ` `` 和 `~`）
- ❌ 围栏前最多 3 个空格缩进
- ❌ 未闭合围栏延伸到文档末尾
- ❌ 围栏代码块内容不解析 Markdown 语法

#### 缩进代码块
- ❌ 4 个空格或 1 个 Tab 缩进
- ❌ 连续缩进行合并为一个代码块
- ❌ 代码块中间允许空行（空行后仍缩进则继续）
- ❌ 不能打断段落（段落后紧跟缩进行仍属于段落）

#### 语法高亮（扩展）
- ❌ 根据 info string 语言标识进行语法高亮

### ✅ 已支持
- 无

**覆盖率**: 0/14 (0%)

---

## 4. 块级结构 — 块引用

### ❌ 待实现
- ❌ `>` 前缀标记块引用
- ❌ `>` 后可选一个空格
- ❌ `>` 前最多 3 个空格缩进
- ❌ 多行块引用（每行 `>` 前缀）
- ❌ Lazy continuation（后续行不带 `>` 也属于同一块引用）
- ❌ 嵌套块引用（`>> ...`）
- ❌ 块引用内包含其他块级元素（标题、列表、代码块等）
- ❌ 空行终止块引用（除非后续行有 `>`）

### ✅ 已支持
- 无

**覆盖率**: 0/8 (0%)

---

## 5. 块级结构 — 列表

### ❌ 待实现

#### 无序列表
- ❌ `-` 标记符
- ❌ `*` 标记符
- ❌ `+` 标记符
- ❌ 标记符后至少 1 个空格
- ❌ 不同标记符产生不同列表（不合并）

#### 有序列表
- ❌ `1.` 标记符（数字 + 点）
- ❌ `1)` 标记符（数字 + 右括号）
- ❌ 起始编号支持（非 1 开头）
- ❌ 数字最多 9 位
- ❌ 不同定界符（`.` 和 `)`）产生不同列表

#### 列表通用
- ❌ 紧凑列表（tight）与松散列表（loose）区分
- ❌ 列表项嵌套（缩进对齐）
- ❌ 列表项包含多个块级元素（段落、代码块等）
- ❌ 列表项 continuation（缩进延续行）
- ❌ 列表项中空行处理（空行使列表变为 loose）
- ❌ 列表可以打断段落（无序列表总是可以，有序列表仅 `1.` 可以）

#### 任务列表（GFM 扩展）
- ❌ `- [ ] ` 未完成任务
- ❌ `- [x] ` 已完成任务
- ❌ `- [X] ` 已完成任务（大小写不敏感）
- ❌ 任务列表嵌套

### ✅ 已支持
- 无

**覆盖率**: 0/20 (0%)

---

## 6. 块级结构 — 分隔线

### ❌ 待实现
- ❌ `---` 三个或更多连字符
- ❌ `***` 三个或更多星号
- ❌ `___` 三个或更多下划线
- ❌ 符号之间允许空格（如 `- - -`、`* * *`）
- ❌ 前面最多 3 个空格缩进
- ❌ 分隔线优先于 Setext 标题（在特定上下文中）

### ✅ 已支持
- 无

**覆盖率**: 0/6 (0%)

---

## 7. 块级结构 — 表格（GFM 扩展）

### ❌ 待实现
- ❌ `|` 分隔列
- ❌ 表头行（第一行）
- ❌ 分隔行（`|---|---|`，仅含 `-`、`:`、`|`、空格）
- ❌ 左对齐 `:---`
- ❌ 右对齐 `---:`
- ❌ 居中对齐 `:---:`
- ❌ 默认对齐（无冒号，左对齐）
- ❌ 单元格内行内元素解析
- ❌ 首尾 `|` 可选
- ❌ 列数以分隔行为准，不足补空，多余截断
- ❌ 单元格内 `|` 转义（`\|` 或 `&#124;`）
- ❌ 表格不能打断段落（需空行隔开）

### ✅ 已支持
- 无

**覆盖率**: 0/12 (0%)

---

## 8. 块级结构 — HTML 块

### ❌ 待实现
- ❌ 类型 1：`<script>`, `<pre>`, `<style>`, `<textarea>`（到对应闭合标签或文档末尾）
- ❌ 类型 2：`<!--` HTML 注释（到 `-->`）
- ❌ 类型 3：`<?` 处理指令（到 `?>`）
- ❌ 类型 4：`<!` 声明（如 `<!DOCTYPE>`，到 `>`）
- ❌ 类型 5：`<![CDATA[`（到 `]]>`）
- ❌ 类型 6：已知块级 HTML 标签（`<div>`, `<table>`, `<p>`, `<h1>`-`<h6>`, `<section>`, `<article>`, `<nav>`, `<header>`, `<footer>`, `<main>`, `<details>`, `<summary>` 等，到空行）
- ❌ 类型 7：其他独立 HTML 标签（到空行）
- ❌ HTML 块前最多 3 个空格缩进
- ❌ HTML 块可以打断段落（类型 1-6）

#### GFM 禁止的原始 HTML（GFM 扩展）
- ❌ 过滤 `<title>`, `<textarea>`, `<style>`, `<xmp>`, `<iframe>`, `<noembed>`, `<noframes>`, `<script>`, `<plaintext>` 标签

### ✅ 已支持
- 无

**覆盖率**: 0/10 (0%)

---

## 9. 块级结构 — 链接引用定义

### ❌ 待实现
- ❌ `[label]: destination` 基本格式
- ❌ `[label]: destination "title"` 带双引号标题
- ❌ `[label]: destination 'title'` 带单引号标题
- ❌ `[label]: destination (title)` 带括号标题
- ❌ `[label]: <destination>` 尖括号包裹 URL
- ❌ destination 可含空格（需尖括号包裹）
- ❌ 标题可跨行（续行）
- ❌ label 大小写不敏感匹配
- ❌ label 折叠连续空白为单个空格
- ❌ 定义不产生输出，仅供引用链接使用
- ❌ 同名 label 取第一个定义
- ❌ 链接引用定义前最多 3 个空格

### ✅ 已支持
- 无

**覆盖率**: 0/12 (0%)

---

## 10. 块级结构 — 扩展

### ❌ 待实现

#### 脚注定义（扩展）
- ❌ `[^label]: content` 单行脚注
- ❌ 多行脚注（续行缩进 4 空格）
- ❌ 脚注内包含块级元素
- ❌ 脚注自动编号

#### 数学公式块（扩展）
- ❌ `$$...$$` 多行数学公式
- ❌ 单行 `$$ content $$`
- ❌ 数学块内不解析 Markdown 语法

#### 定义列表（扩展）
- ❌ 术语行（紧接定义前的非空行）
- ❌ `: definition` 定义行（冒号 + 空格 + 定义内容）
- ❌ 一个术语多个定义
- ❌ 多个术语共享定义
- ❌ 定义内包含块级元素（多段落、代码块等）

#### 告示/提醒块（Admonition，扩展）
- ❌ `> [!NOTE]` 提示块
- ❌ `> [!TIP]` 技巧块
- ❌ `> [!IMPORTANT]` 重要块
- ❌ `> [!WARNING]` 警告块
- ❌ `> [!CAUTION]` 注意块

#### 目录（TOC，扩展）
- ❌ `[TOC]` 自动生成目录
- ❌ `[[toc]]` 自动生成目录（备选语法）

#### Front Matter（扩展）
- ❌ `---\n...\n---` YAML front matter
- ❌ `+++\n...\n+++` TOML front matter

#### 可折叠内容（扩展）
- ❌ `<details><summary>标题</summary>内容</details>` 折叠/展开块

### ✅ 已支持
- 无

**覆盖率**: 0/22 (0%)

---

## 11. 行内元素 — 强调

### ❌ 待实现

#### 斜体
- ❌ `*text*` 星号斜体
- ❌ `_text_` 下划线斜体
- ❌ 左侧 flanking delimiter run 规则
- ❌ 右侧 flanking delimiter run 规则
- ❌ `_` 不在单词内部生效（CommonMark 规则：`foo_bar_baz` 不解析）

#### 粗体
- ❌ `**text**` 星号粗体
- ❌ `__text__` 下划线粗体

#### 粗斜体
- ❌ `***text***` 三星号粗斜体
- ❌ `___text___` 三下划线粗斜体

#### 嵌套
- ❌ 粗体内嵌斜体 `**bold *italic* bold**`
- ❌ 斜体内嵌粗体 `*italic **bold** italic*`
- ❌ 多层嵌套强调
- ❌ 强调定界符的 "多个3的规则"（sum of delimiter lengths not multiple of 3）

### ✅ 已支持
- 无

**覆盖率**: 0/13 (0%)

---

## 12. 行内元素 — 删除线（GFM 扩展）

### ❌ 待实现
- ❌ `~~text~~` 双波浪线删除线
- ❌ `~text~` 单波浪线删除线（部分处理器支持）
- ❌ 删除线内嵌套其他行内元素
- ❌ 三个及以上波浪线不产生删除线

### ✅ 已支持
- 无

**覆盖率**: 0/4 (0%)

---

## 13. 行内元素 — 代码

### ❌ 待实现
- ❌ `` `code` `` 单反引号行内代码
- ❌ ` `` code `` ` 双反引号（内容含单反引号时）
- ❌ 多反引号匹配（开闭反引号数量须一致）
- ❌ 开闭标记后/前的单个空格剥离（`` ` `` foo `` ` `` → `foo`）
- ❌ 仅含空格的代码段不做空格剥离
- ❌ 行内代码不解析内部 Markdown 语法
- ❌ 行内代码中换行转为空格
- ❌ 反引号串未匹配时按字面文本处理

### ✅ 已支持
- 无

**覆盖率**: 0/8 (0%)

---

## 14. 行内元素 — 链接

### ❌ 待实现

#### 行内链接
- ❌ `[text](url)` 基本链接
- ❌ `[text](url "title")` 带双引号标题
- ❌ `[text](url 'title')` 带单引号标题
- ❌ `[text](url (title))` 带括号标题
- ❌ `[text](<url>)` 尖括号 URL（可含空格）
- ❌ `[text]()` 空 URL
- ❌ URL 中括号的平衡匹配
- ❌ URL 中特殊字符百分号编码
- ❌ 链接文本内嵌套行内元素（粗体、斜体、代码、图片等）
- ❌ 链接不能嵌套链接

#### 引用链接
- ❌ `[text][label]` 完整引用链接
- ❌ `[label][]` 折叠引用链接
- ❌ `[label]` 简写引用链接
- ❌ label 大小写不敏感匹配
- ❌ label 折叠连续空白

#### 自动链接
- ❌ `<url>` 尖括号自动链接（绝对 URI）
- ❌ `<email@example.com>` 邮箱自动链接
- ❌ GFM autolink（裸 URL 自动识别，如 `https://example.com`）
- ❌ GFM autolink（裸 `www.` 前缀自动识别）
- ❌ GFM autolink（裸邮箱自动识别）
- ❌ GFM autolink 尾部标点截断规则（不含尾部 `.`, `)`, `;` 等）

### ✅ 已支持
- 无

**覆盖率**: 0/21 (0%)

---

## 15. 行内元素 — 图片

### ❌ 待实现
- ❌ `![alt](url)` 基本图片
- ❌ `![alt](url "title")` 带标题图片
- ❌ `![alt](<url>)` 尖括号 URL 图片
- ❌ `![alt][label]` 引用式图片
- ❌ `![alt][]` 折叠引用图片
- ❌ `![alt]` 简写引用图片
- ❌ alt 文本内行内元素解析（粗体、斜体、代码等）
- ❌ alt 文本中嵌套图片时提取纯文本
- ❌ 图片可嵌套在链接内 `[![alt](img-url)](link-url)`

### ✅ 已支持
- 无

**覆盖率**: 0/9 (0%)

---

## 16. 行内元素 — HTML

### ❌ 待实现
- ❌ 行内开标签 `<tag>` / `<tag attr="value">`（含属性）
- ❌ 行内闭标签 `</tag>`
- ❌ 自闭合标签 `<br />` / `<img />`
- ❌ 行内 HTML 注释 `<!-- comment -->`
- ❌ 行内处理指令 `<? ... ?>`
- ❌ 行内 CDATA `<![CDATA[ ... ]]>`
- ❌ 行内声明 `<!DECLARATION>`
- ❌ 行内 HTML 内不解析 Markdown 语法

### ✅ 已支持
- 无

**覆盖率**: 0/8 (0%)

---

## 17. 行内元素 — 转义与实体

### ❌ 待实现

#### 转义字符
- ❌ `\` + ASCII 标点符号转义（共 32 个可转义字符）：
  `` \` \~ \! \@ \# \$ \% \^ \& \* \( \) \- \_ \= \+ \[ \] \{ \} \| \\ \; \: \' \" \, \. \< \> \/ \? ``
- ❌ 转义在围栏代码块中无效
- ❌ 转义在行内代码中无效
- ❌ 转义在自动链接中无效
- ❌ `\` + 换行 = 硬换行

#### HTML 实体
- ❌ 命名实体（`&amp;` `&lt;` `&gt;` `&nbsp;` `&copy;` `&mdash;` `&hellip;` 等，HTML5 完整列表）
- ❌ 十进制数字实体（`&#123;` `&#9829;`）
- ❌ 十六进制数字实体（`&#x1F4A9;` `&#xA9;`）
- ❌ 无效实体按字面文本处理（`&foo;`）
- ❌ 实体在代码块/行内代码中不解析

### ✅ 已支持
- 无

**覆盖率**: 0/10 (0%)

---

## 18. 行内元素 — 换行

### ❌ 待实现
- ❌ 硬换行：行尾两个或更多空格 + 换行符
- ❌ 硬换行：行尾反斜杠 `\` + 换行符
- ❌ 软换行：普通换行符（渲染为空格或换行，取决于渲染器）
- ❌ 硬换行不在块级元素结尾生效（段落/标题最后一行无效）
- ❌ 硬换行不在行内代码中生效

### ✅ 已支持
- 无

**覆盖率**: 0/5 (0%)

---

## 19. 行内元素 — 扩展

### ❌ 待实现

#### 脚注引用（扩展）
- ❌ `[^label]` 脚注引用标记
- ❌ 脚注引用渲染为上标编号
- ❌ 点击脚注跳转到脚注定义

#### 行内数学（扩展）
- ❌ `$...$` 单美元符行内数学
- ❌ `$$...$$` 双美元符行内数学（不独占一行时为行内）
- ❌ 数学公式内不解析 Markdown 语法
- ❌ `$` 紧邻数字时不触发数学模式（避免 `$100` 误解析）

#### 高亮（扩展）
- ❌ `==text==` 双等号高亮标记
- ❌ 高亮内嵌套其他行内元素

#### 上标（扩展）
- ❌ `^text^` 上标标记
- ❌ `<sup>text</sup>` HTML 上标

#### 下标（扩展）
- ❌ `~text~` 下标标记（需与删除线区分）
- ❌ `<sub>text</sub>` HTML 下标

#### 插入文本（扩展）
- ❌ `++text++` 插入标记（渲染为下划线/插入样式）

#### Emoji（扩展）
- ❌ `:emoji_name:` 短代码 Emoji（如 `:smile:` → 😄）
- ❌ Unicode Emoji 直接支持（如 `😀` 直接渲染）

#### 缩写（Abbreviation，扩展）
- ❌ `*[abbr]: Full Text` 缩写定义
- ❌ 正文中出现 `abbr` 时自动添加提示（tooltip）

#### 键盘按键（扩展）
- ❌ `<kbd>Ctrl</kbd>` 键盘按键标记

### ✅ 已支持
- 无

**覆盖率**: 0/21 (0%)

---

## 20. 增量解析引擎

### ❌ 待实现

#### 编辑操作
- ❌ `Insert` 插入操作（位置 + 内容）
- ❌ `Delete` 删除操作（范围）
- ❌ `Replace` 替换操作（范围 + 新内容）

#### 变更追踪
- ❌ 行级变更检测（新增/删除/修改行）
- ❌ 脏区域标记（受影响的行范围）
- ❌ 脏区域到 AST 块节点的映射

#### 增量调度
- ❌ 脏区域小 → 局部重解析
- ❌ 脏区域大 → 退化为全量解析
- ❌ 跨块元素的脏区域扩展（围栏代码块、块引用等）

#### AST 补丁
- ❌ 新子树替换旧子树
- ❌ 后续节点 `sourceRange` 偏移修正
- ❌ 后续节点 `lineRange` 偏移修正

### ✅ 已支持
- 无

**覆盖率**: 0/12 (0%)

---

## 21. 字符与编码

### ❌ 待实现
- ❌ UTF-8 编码支持
- ❌ U+0000（NULL）替换为 U+FFFD（REPLACEMENT CHARACTER）
- ❌ 制表符（Tab）展开为空格（对齐到 4 的倍数列）
- ❌ 行尾序列标准化（`\r\n`、`\r`、`\n` 统一为 `\n`）
- ❌ Unicode 标点符号识别（用于强调定界符规则）
- ❌ Unicode 空白字符识别（用于强调定界符规则）

### ✅ 已支持
- 无

**覆盖率**: 0/6 (0%)

---

## 📊 总体覆盖率

| # | 类别 | 已支持 | 缺失 | 覆盖率 |
|---|------|--------|------|--------|
| 1 | 标题 | 0/17 | 17 | 0% |
| 2 | 段落与空行 | 0/5 | 5 | 0% |
| 3 | 代码块 | 0/14 | 14 | 0% |
| 4 | 块引用 | 0/8 | 8 | 0% |
| 5 | 列表 | 0/20 | 20 | 0% |
| 6 | 分隔线 | 0/6 | 6 | 0% |
| 7 | 表格（GFM） | 0/12 | 12 | 0% |
| 8 | HTML 块 | 0/10 | 10 | 0% |
| 9 | 链接引用定义 | 0/12 | 12 | 0% |
| 10 | 块级扩展 | 0/22 | 22 | 0% |
| 11 | 强调 | 0/13 | 13 | 0% |
| 12 | 删除线（GFM） | 0/4 | 4 | 0% |
| 13 | 行内代码 | 0/8 | 8 | 0% |
| 14 | 链接 | 0/21 | 21 | 0% |
| 15 | 图片 | 0/9 | 9 | 0% |
| 16 | 行内 HTML | 0/8 | 8 | 0% |
| 17 | 转义与实体 | 0/10 | 10 | 0% |
| 18 | 换行 | 0/5 | 5 | 0% |
| 19 | 行内扩展 | 0/21 | 21 | 0% |
| 20 | 增量解析 | 0/12 | 12 | 0% |
| 21 | 字符与编码 | 0/6 | 6 | 0% |
| | **总计** | **0/243** | **243** | **0%** |

---

## 🎯 规范参考

| 规范 | 说明 | 涉及章节 |
|------|------|----------|
| [CommonMark Spec 0.31.2](https://spec.commonmark.org/0.31.2/) | Markdown 核心规范 | 1-9, 11, 13-18, 21 |
| [GFM Spec 0.29](https://github.github.com/gfm/) | GitHub Flavored Markdown | 5（任务列表）、7（表格）、8（禁止 HTML）、12（删除线）、14（自动链接扩展） |
| [Markdown Guide Extended Syntax](https://www.markdownguide.org/extended-syntax/) | 社区扩展语法参考 | 1（标题 ID）、10（定义列表/告示/TOC/Front Matter）、19（高亮/上标/下标/Emoji/缩写） |
| 扩展语法（社区约定） | 脚注、数学公式、插入文本、键盘 | 10, 19 |

---

## 📋 实施优先级

| 优先级 | 类别 | 说明 |
|--------|------|------|
| **P0** | 字符与编码、段落、标题、分隔线、换行 | 最基础结构，其他一切的前提 |
| **P1** | 强调、行内代码、链接、图片、转义与实体 | 核心行内元素 |
| **P2** | 代码块、块引用、列表 | 核心块级容器 |
| **P3** | 表格、HTML 块、链接引用定义、删除线 | GFM 扩展 + 引用体系 |
| **P4** | 行内扩展（数学、高亮、脚注引用、上标/下标、Emoji） | 常用行内扩展 |
| **P5** | 块级扩展（脚注定义、数学块、定义列表、告示、TOC、Front Matter） | 块级扩展语法 |
| **P6** | 标题 ID、缩写、插入文本、键盘按键、可折叠内容 | 低频扩展 |
| **P7** | 增量解析引擎 | 性能优化（全量解析跑通后再做） |
